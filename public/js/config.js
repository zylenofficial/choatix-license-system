/* Phantom V2 - Deployment configuration
 * ------------------------------------------------------------------
 * Leave '' (empty) when the frontend is served by the Node server
 * itself (local dev: http://localhost:3000, or full deploy on Render).
 *
 * When the frontend is hosted on GitHub Pages, set this to your
 * Render backend URL, for example:
 *   window.PHANTOM_API_BASE = 'https://choatix-license-system.onrender.com';
 */
window.PHANTOM_API_BASE = '';
